//var can be re-declared and updated
let fullName = "Harry Styles"; //cannot be re-declared but can be updated
const age = "30"; // cannot be re-declared or updated
console.log(fullName);
console.log(age);

x = null;
y = undefined; //if any variable is declared and not given any value; undefined
console.log(x);
console.log(y); 

//let can be un-initialised but const cannot be.